class SignupPage extends StatelessWidget {
  // Similar structure to LoginPage with:
  // - Additional fields (name, confirm password)
  // - Firebase user creation logic
  // - Navigation to profile after signup
}

class StatelessWidget {
}